<section id="start-view" class="view active">
        <h1>Chose Your Quiz</h1>
        <div class="quiz-selection">
            {{#each quizzes}}
            <a class="quiz-btn" data-quiz-id="1" href="/quizzes/{{id}}">{{name}}</a>
            {{/each}}
        </div>
    </section>